apiVersion: v1
kind: Service
metadata:
  name: nginx-custom-site-service
spec:
  type: NodePort
  selector:
    app: nginx-custom-site
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080 
      nodePort: 30080 